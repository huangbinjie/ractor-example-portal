export type AppToken = {
  access_token: string
  expire_time: number
  expires_in: number
  refresh_token: string
  refresh_expires_in: number
  token_type: string
  session_id: string
  client_access_time: number,
  client_expires_time: number
}